
export enum TabRoute {
    ANONYMOUS = "anonymousTab",
    HOME = "homeTab",
    DISCOVERY = "discoveryTab",
    QUIZ = "quizTab",
}

export enum ViewRoute {
    LANDING = "landing",
    HOME = "home",
    PROGRESS = "progress",
    LOBBY = "lobby",
}
