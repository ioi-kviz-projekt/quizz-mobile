
export enum TabRoute {
    ANONYMOUS = "anonymous",
    HOME = "home",
    DISCOVERY = "discovery",
    QUIZ = "quiz",
}

export enum ViewRoute {
    LANDING = "landing",
    HOME = "home",
    PROGRESS = "progress",
    LOBBY = "lobby",
}
