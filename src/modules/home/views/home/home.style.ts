import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    content: {
        marginTop: 40,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        height: "100%",
    },
    firstButton: {
        marginBottom: 50,
    },
    logoutButton: {
        marginTop: 50,
    },
});
