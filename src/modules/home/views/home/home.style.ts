import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    content: {
        marginTop: 40,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
    },
    firstButton: {
        marginBottom: 50,
    },
});
